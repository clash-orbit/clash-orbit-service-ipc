name: Release CI

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Version tag to release, for example v2.2.0"
        required: true
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  prepare:
    name: Prepare Release
    runs-on: ubuntu-26.04
    outputs:
      tag: ${{ steps.release.outputs.tag }}
      version: ${{ steps.release.outputs.version }}
      asset_prefix: ${{ steps.release.outputs.asset_prefix }}
      prerelease: ${{ steps.release.outputs.prerelease }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
          fetch-depth: 0

      - name: Validate release tag
        id: release
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag="${{ inputs.tag }}"
          else
            tag="${GITHUB_REF_NAME}"
          fi

          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid release tag: $tag" >&2
            exit 1
          fi

          version="${tag#v}"
          cargo_version="$(awk -F '"' '/^version = / { print $2; exit }' Cargo.toml)"
          if [ "$cargo_version" != "$version" ]; then
            echo "Cargo.toml version ($cargo_version) does not match release tag ($version)" >&2
            exit 1
          fi

          prerelease=false
          if [[ "$version" == *-* ]]; then
            prerelease=true
          fi

          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "asset_prefix=clash-orbit-service-ipc-$tag" >> "$GITHUB_OUTPUT"
          echo "prerelease=$prerelease" >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.target }}
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: windows-latest
            target: i686-pc-windows-msvc
          - os: windows-latest
            target: aarch64-pc-windows-msvc
          - os: ubuntu-26.04
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-26.04
            target: i686-unknown-linux-gnu
          - os: ubuntu-26.04
            target: aarch64-unknown-linux-gnu
          - os: ubuntu-26.04
            target: armv7-unknown-linux-gnueabihf
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.prepare.outputs.tag }}
          fetch-depth: 0

      - name: Install Rust
        if: matrix.os != 'ubuntu-26.04'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Add Rust target
        if: matrix.os != 'ubuntu-26.04'
        run: rustup target add ${{ matrix.target }}

      - name: Build
        if: matrix.os != 'ubuntu-26.04'
        run: cargo build --release --target ${{ matrix.target }} --features "standalone client" --verbose

      - name: Build Linux
        if: matrix.os == 'ubuntu-26.04'
        uses: ./.github/build-for-linux
        with:
          target: ${{ matrix.target }}
          features: standalone,client

      - name: Package Windows binaries
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $asset = "${{ needs.prepare.outputs.asset_prefix }}-${{ matrix.target }}"
          New-Item -ItemType Directory -Path package -Force | Out-Null
          New-Item -ItemType Directory -Path dist -Force | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/clash-verge-service.exe" package/
          Copy-Item "target/${{ matrix.target }}/release/clash-verge-service-install.exe" package/
          Copy-Item "target/${{ matrix.target }}/release/clash-verge-service-uninstall.exe" package/
          Copy-Item LICENSE package/
          Compress-Archive -Path package/* -DestinationPath "dist/$asset.zip" -Force

      - name: Package Unix binaries
        if: runner.os != 'Windows'
        shell: bash
        run: |
          asset="${{ needs.prepare.outputs.asset_prefix }}-${{ matrix.target }}"
          mkdir -p package dist
          cp "target/${{ matrix.target }}/release/clash-verge-service" package/
          cp "target/${{ matrix.target }}/release/clash-verge-service-install" package/
          cp "target/${{ matrix.target }}/release/clash-verge-service-uninstall" package/
          cp LICENSE package/
          tar -czf "dist/$asset.tar.gz" -C package .

      - name: Upload release artifact
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ matrix.target }}
          path: |
            dist/*.zip
            dist/*.tar.gz
          if-no-files-found: error

  release:
    name: Publish Release
    needs: [prepare, build]
    runs-on: ubuntu-26.04
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: release-*
          path: dist
          merge-multiple: true

      - name: Generate SHA256SUMS
        shell: bash
        run: |
          cd dist
          sha256sum * > SHA256SUMS
          cat SHA256SUMS

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: clash-orbit-service-ipc ${{ needs.prepare.outputs.tag }}
          prerelease: ${{ needs.prepare.outputs.prerelease }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          overwrite_files: true
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
